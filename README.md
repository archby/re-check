# Re-Check

Lightweight and memory efficient Python CLI tool that scans directories and tracks duplicate files.

*Installation:*
```bash
git clone https://github.com/archby/re-check.git
cd re-check
python main.py
```
